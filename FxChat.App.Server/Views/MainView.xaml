<Window
    x:Class="FxChat.App.Server.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:FxChat.App.Server.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainView"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel
            Grid.Row="0"
            Margin="5"
            Orientation="Horizontal">
            <Label>Ip:</Label>
            <TextBox Width="200" Text="{Binding ServerIp}" />
            <Label Margin="10,0,0,0">Port:</Label>
            <TextBox Width="200" Text="{Binding ServerPort}" />

            <Button Margin="10,0,0,0" Command="{Binding StartServerCommand}">启用Socket服务</Button>
        </StackPanel>

        <ListView
            Grid.Row="1"
            Margin="5"
            ItemsSource="{Binding SocketClients}" />

    </Grid>
</Window>
