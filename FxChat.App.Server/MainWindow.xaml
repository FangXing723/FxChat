<Window
    x:Class="FxChat.App.Server.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:FxChat.App.Server"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition Height="40" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Click="Button_Click">启动服务</Button>

        <ListBox
            x:Name="connectList"
            Grid.Row="1"
            Margin="5" />

        <RichTextBox
            x:Name="textBox"
            Grid.Row="2"
            Margin="5" />

        <StackPanel Grid.Row="3" Orientation="Horizontal">
            <TextBox
                x:Name="sendTextBox"
                Width="300"
                Margin="5,0,0,0"
                Text="Hello!" />

            <Button
                Name="sendButton"
                Width="50"
                Click="sendButton_Click">
                发送
            </Button>
        </StackPanel>
    </Grid>
</Window>
